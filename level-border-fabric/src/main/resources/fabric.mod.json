{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${modVersion}",
  "name": "${modName}",
  "description": "${modDescription}",
  "license": "${modLicense}",
  "contact": {
    "homepage": "${websiteUrl}",
    "issues": "${issuesUrl}"
  },
  "authors": [
    "${modAuthor}"
  ],
  "mixins": [
    "levelborder.mixins.json"
  ],
  "environment": "*",
  "depends": {
    "minecraft": "${minecraftVersion}.x",
    "fabricloader": ">=0.8.7"
  }
}
